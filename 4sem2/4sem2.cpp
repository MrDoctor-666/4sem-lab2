#include "SFCode.h"

int main()
{
	/*SFCode* test = new SFCode(string, n);
	cout << "Коды: ";
	test->CodesOut();
	bool* code = test->CodeString();
	cout << endl << "Закодированная строка: ";
	for (int i = 0; i < test->GetCodeLength(); i++) cout << code[i];
	cout << endl;
	cout << "Раскодированная строка: ";
	test->DecodeString();
	cout << endl << "Коэффициент сжатия: " << test->Coeff();*/
}
